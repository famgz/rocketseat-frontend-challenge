import styled from "styled-components";

export const Divider = styled.hr`
    width: 100%;
    height: 1px;
    margin: 8px auto;
    border-style: none;
    padding: 0;
    background: var(--shapes);
`
